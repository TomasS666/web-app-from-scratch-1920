function Home(){
    //  genreList.forEach(obj => {
    //     Genre(`with_genres=${obj.id}`, obj.name)
    // })

    Genre(`with_genres=${27}`, {
        id:27,
        name: "Horror"
    })

    return;
}

export { Home }